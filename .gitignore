# Nix build outputs
result
result-*

# OVMF EFI variables written by the interactive test VM (`nix run .#vm`)
*-efi-vars.fd

# direnv / nix-direnv
.direnv/
.envrc.local

# git-hooks.nix generated config (a nix-store symlink written by the devShell's
# pre-commit installationScript; regenerated per checkout, never committed)
.pre-commit-config.yaml

# Editor / OS cruft
.DS_Store
*.swp
*~

# Claude Code — ignore personal/local settings, keep shared config
# (CLAUDE.md, .claude/settings.json, and .claude/{commands,agents}/ are committable)
.claude/settings.local.json
